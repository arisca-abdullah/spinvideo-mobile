# Setup Project

1. Install Dependencies
    ```
    npm i
    ```

2. Replace node_modules/@capacitor/filesystem with capacitor_plugins/filesystem

3. Sync Project
    ```
    ionic cap sync
    ionic cap sync --prod
    ```
